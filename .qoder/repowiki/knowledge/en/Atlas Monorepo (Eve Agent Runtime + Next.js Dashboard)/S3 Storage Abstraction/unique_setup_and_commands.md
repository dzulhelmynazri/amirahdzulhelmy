Requires runtime environment variables `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`, `S3_ENDPOINT`, `S3_REGION`, and `S3_ENCRYPTION_KEY` (base64-encoded) to instantiate the storage; no build or test scripts are defined in `package.json`.
